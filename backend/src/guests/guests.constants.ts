/** Tag name used when «Обнаружить дубликаты» finds similar guests (per EventType). */
export const DUPLICATE_GUEST_TAG_NAME = 'Дубликаты'

/** Max Levenshtein distance for fuzzy name match (inclusive). */
export const DUPLICATE_LEVENSHTEIN_THRESHOLD = 2
